<field:FieldControlBase x:Class="JosephM.Wpf.Grid.GridPicklistFieldView"
                        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                        xmlns:field="clr-namespace:JosephM.Wpf.RecordEntry.Field"
                        mc:Ignorable="d">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/JosephM.Wpf;component/Resources/Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <ComboBox
        Name="ComboBox"
        Style="{StaticResource GridFieldEntryStyle}"
        DisplayMemberPath="Value"
        ItemsSource="{Binding ItemsSource, Mode=TwoWay}"
        IsEnabled="{Binding IsVisibleAndEditable}"
        SelectedItem="{Binding Value, Mode=TwoWay, NotifyOnValidationError=True}" />
</field:FieldControlBase>