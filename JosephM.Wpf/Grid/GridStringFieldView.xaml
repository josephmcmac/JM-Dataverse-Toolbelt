<UserControl x:Class="JosephM.Wpf.Grid.GridStringFieldView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:JosephM.Wpf.Converters"
             mc:Ignorable="d"
             d:DesignHeight="50" d:DesignWidth="400">

    <UserControl.Resources>
        <ResourceDictionary>
            <converters:BooleanToFalseConverter x:Key="BoolToFalse" />
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/JosephM.Wpf;component/Resources/Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <TextBox
        Style="{StaticResource GridStringField}"
        Text="{Binding Value, Mode=TwoWay}"
        IsReadOnly="{Binding IsVisibleAndEditable, Converter={StaticResource BoolToFalse}, Mode=OneWay}"
        Margin="0" />
</UserControl>