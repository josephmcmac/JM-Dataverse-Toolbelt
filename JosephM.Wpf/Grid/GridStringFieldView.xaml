<UserControl x:Class="JosephM.Wpf.Grid.GridStringFieldView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:JosephM.Wpf.Converters"
             mc:Ignorable="d"
             d:DesignHeight="50" d:DesignWidth="400">

    <UserControl.Resources>
        <ResourceDictionary>
            <BooleanToVisibilityConverter x:Key="BoolToVis" />
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/JosephM.Wpf;component/Resources/Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <TextBox
        AcceptsReturn="{Binding IsMultiline}"
            MinLines ="1"
            MaxLines ="1"
            TabIndex="1"
            Name="TextBox"
            DataContext="{Binding NotifyOnValidationError=True}"
            Text="{Binding Mode=TwoWay, Path=ValueObject}"
            Style="{StaticResource FieldEntryStyle}"
            IsEnabled="{Binding IsEditable, Mode=TwoWay}"
            Visibility="{Binding IsVisible, Mode=TwoWay, Converter={StaticResource BoolToVis}}" />
</UserControl>